
  <ul id="dropdown1" class="dropdown-content">
  <% if current_user %>
    <%current_user.groups.each do |group|%>
      <li class="drop-down-links"><%=link_to group.name, group_path(group)%></li>
      <li class="divider"></li>
      <%end%>
  <%end%>

  </ul>
  <nav>
    <div class="nav-wrapper red lighten-1">
      <div class="hide-on-large-only"><a href="/" class="brand-logo">TaskMate</a></div>

      <div class="hide-on-small-only"><a href="/" class="brand-logo move-right">TaskMate</a></div>
      
      <a href="#" data-activates="mobile-demo" class="button-collapse"><i class="fa fa-bars" aria-hidden="true"></i></a>
      <ul class="right hide-on-med-and-down nav-link-format">
      <% if user_signed_in? %>
        <li><a class="dropdown-button" href="#!" data-activates="dropdown1">Groups</a></li>
        <li><%= link_to current_user.name, user_path(current_user)%></li>
        <li><a href="#">Completions</a></li>
        <li><%= link_to "Log Out", destroy_user_session_path%></li>
      <% else %>
        <li><a href="#" class="side-links">Log In</a></li>
      <% end %>
      </ul>
      <ul class="side-nav" id="mobile-demo">
        <% if user_signed_in? %>
          <li><a href="#" class="side-links">Group</a></li>
          <li><%= link_to current_user.name, user_path(current_user)%></li>
          <li><a href="#" class="side-links">Completions</a></li>
          <li><a href="#" class="side-links">Log Out</a></li>
        <% else %>
          <li><a href="#" class="side-links">Log In</a></li>
        <% end %>
      </ul>
    </div>
  </nav>
