
  <nav>
    <ul id="user-groups" class="dropdown-content">
  <% if current_user %>
    <%current_user.groups.each do |group|%>
      <li class="divider"></li>
      <li class="drop-down-links"><%=link_to group.name, group_path(group)%></li>
      <%end%>
  <%end%>
  </ul>


    <div class="nav-wrapper red lighten-1">
      <div class="hide-on-large-only"><a href="/" class="brand-logo">TaskMate</a></div>

      <div class="hide-on-small-only"><a href="/" class="brand-logo move-right">TaskMate</a></div>
      
      <a data-activates="mobile-demo" class="button-collapse"><i class="fa fa-bars" aria-hidden="true"></i></a>
      <ul class="right hide-on-med-and-down nav-link-format">
      <% if current_user %>
        <li><a class="dropdown-button" data-activates="user-groups">Groups</a></li>
        <li><%= link_to "New Groups", new_group_path %></li>
        <li><%= link_to current_user.name, user_path(current_user)%></li>
        <li><%= link_to "Log Out", destroy_user_session_path, :method => :delete %></li>
      <% else %>
        <li><%= link_to "Log In", user_session %></li>
      <% end %>
      </ul>
      <ul class="side-nav" id="mobile-demo">
        <% if current_user %>
          <li><a href="#" class="side-links">Groups</a></li>
          <li><%= link_to current_user.name, user_path(current_user)%></li>
          <li><%= link_to "Log Out", destroy_user_session_path, :method => :delete %></li>
        <% else %>
          <li><%= link_to "Log In", user_session %></li>
        <% end %>
      </ul>
    </div>
  </nav>
