<%= form_for [@task,@completion], remote: true, :html => {class: "completion-checkbox" } do |f| %>
  <%= f.check_box :completed, id:'completed_checkbox' %>

  <%= f.label :check_to_complete, for:"completed_checkbox" %> 

<% end %>
