
<%= form_for @assignment do |f| %>
	<div><%= f.hidden_field :task_id,  value: @task.id %></div>
	<%=f.collection_select(:assignee_id, @task.group.users, :id, :name, {}, class: "browser-default")%>
	
    <%= f.submit "Assign task" %>
<% end %>